# Copy to .env and fill in real values. Never commit .env.

# --- LLM (Groq, OpenAI-compatible) ---
LLM_API_KEY=
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.3-70b-versatile

# --- Messaging (iMessage) ---
MESSAGING_PROVIDER=sendblue
SENDBLUE_API_KEY=
SENDBLUE_API_SECRET=
# Dedicated Sendblue sending number for the agent, E.164
SENDBLUE_FROM_NUMBER=+18722964991
# The only sender the agent responds to, E.164
ALLOWLISTED_PHONE=
WEBHOOK_SHARED_SECRET=

# --- Storage / scheduling ---
DATABASE_URL=sqlite:///./restaurant_agent.db
TIMEZONE=America/Los_Angeles
MONITOR_CRON_HOUR=9

# --- Restaurants / availability ---
RESTAURANTS_CONFIG=./restaurants.yaml
# cloud_browser | mock
AVAILABILITY_PROVIDER=cloud_browser
# browserbase | steel
BROWSER_PROVIDER=browserbase
BROWSER_API_KEY=
# Scrape rendered slot buttons when no availability response is captured
AVAILABILITY_DOM_FALLBACK=true

LOG_LEVEL=INFO
